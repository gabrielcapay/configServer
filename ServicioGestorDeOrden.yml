server:
  port: 8055

spring:
  application:
    name: ServicioGestorDeOrden
  datasource:
    url: jdbc:mysql://localhost:3306/orden?useSSL=false&serverTimezone=UTC
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
